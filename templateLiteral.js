
let product = {
    name : 'pen',
    price : 10
}

console.log(`The price of the ${product.name} is ${product.price} dbt`)
console.log('Price of the', product.name, 'is', product.price, 'bdt')


let exm = `This is a template literal ${4*4}`
console.log(exm)